module.exports = {
    info: {
        version: '0.1.0',
        title: 'CMU-SV UX Analytics',
    },
    baseDir: __dirname,
    // Glob pattern to find your jsdoc files (multiple patterns can be added in an array)
    filesPattern: '../routes/*.js',
    swaggerUIPath: '/api-docs',
    exposeSwaggerUI: true,
};
